<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pythongames">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PythonGames"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.DetalheEletronicos"></activity>
        <activity android:name=".Activities.DetalheConsole" />
        <activity android:name=".Activities.DetalheJogo" />
        <activity
            android:name=".Activities.DetalhesCarrinho"
            android:label="Detalhes" />
        <activity
            android:name=".Activities.TesteGames"
            android:label="Python Games" />
        <activity
            android:name=".Activities.FormaPagamento"
            android:label="Formas de pagamento" />
        <activity
            android:name=".Activities.Eletronicos"
            android:label="Eletrônicos" />
        <activity
            android:name=".Activities.Console"
            android:label="Consoles" />
        <activity
            android:name=".Activities.Jogos"
            android:label="Jogos" />
        <activity
            android:name=".Activities.Detalhes"
            android:label="Detalhes" />
        <activity
            android:name=".Activities.Carrinho"
            android:label="Carrinho" />
        <activity
            android:name=".Activities.Cadastro"
            android:theme="@style/Theme.PythonGames.NoActionBar" />
        <activity
            android:name=".Activities.Login"
            android:theme="@style/Theme.PythonGames.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PythonGames.NoActionBar">

        </activity>
    </application>

</manifest>